const SUPABASE_URL = "https://dsjskpqdofuhkzkylxqt.supabase.co";
const SUPABASE_PUBLISHABLE_KEY = "sb_publishable_v4Vaxfo6i2Y_E2N24xO0ag_jkprC_Rk";
const supabaseClient = window.supabase && SUPABASE_URL && SUPABASE_PUBLISHABLE_KEY
  ? window.supabase.createClient(SUPABASE_URL, SUPABASE_PUBLISHABLE_KEY) : null;

const state={orders:[],selectedId:null,filter:"active",soundOn:true,channel:null,alertTimeout:null,audioContext:null};
const $=s=>document.querySelector(s), $$=s=>[...document.querySelectorAll(s)];
const money=v=>`$${Number(v||0).toFixed(2)}`;
const esc=v=>String(v??"").replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;").replaceAll("'","&#039;");
const orderNumber=id=>`#${String(id).replaceAll("-","").slice(0,6).toUpperCase()}`;
const statusLabel=s=>String(s||"").replace("_"," ").toUpperCase();
const formatTime=v=>new Intl.DateTimeFormat("en-US",{hour:"numeric",minute:"2-digit"}).format(new Date(v));
const formatDateTime=v=>new Intl.DateTimeFormat("en-US",{month:"short",day:"numeric",hour:"numeric",minute:"2-digit"}).format(new Date(v));
const todayStart=()=>{const d=new Date();d.setHours(0,0,0,0);return d.toISOString()};
const isActive=s=>["new","preparing","ready"].includes(s);

function setConnection(live){const el=$("#connection-status");el.textContent=live?"● Live":"● Reconnecting…";el.classList.toggle("live",live);el.classList.toggle("reconnecting",!live)}
function showToast(m){const t=$("#toast");t.textContent=m;t.classList.remove("hidden");clearTimeout(state.toastTimeout);state.toastTimeout=setTimeout(()=>t.classList.add("hidden"),3500)}
function playNotification(){if(!state.soundOn)return;try{const A=window.AudioContext||window.webkitAudioContext;if(!A)return;state.audioContext ||= new A;const c=state.audioContext;if(c.state==="suspended")c.resume().catch(()=>{});const n=c.currentTime;[0,.14].forEach((o,i)=>{const x=c.createOscillator(),g=c.createGain();x.frequency.value=i?880:660;x.type="sine";g.gain.setValueAtTime(.0001,n+o);g.gain.exponentialRampToValueAtTime(.12,n+o+.015);g.gain.exponentialRampToValueAtTime(.0001,n+o+.12);x.connect(g).connect(c.destination);x.start(n+o);x.stop(n+o+.13)})}catch(_){}}
function showNewOrderAlert(o){$("#new-order-alert-text").textContent=`${orderNumber(o.id)} • ${o.customer_name} • ${money(o.total)}`;$("#new-order-alert").classList.remove("hidden");clearTimeout(state.alertTimeout);state.alertTimeout=setTimeout(()=>$("#new-order-alert").classList.add("hidden"),7000);playNotification()}
function filteredOrders(){return [...state.orders].filter(o=>state.filter==="active"?isActive(o.status):state.filter==="all"?true:o.status===state.filter).sort((a,b)=>new Date(b.created_at)-new Date(a.created_at))}
function updateStats(){$("#stat-new").textContent=state.orders.filter(o=>o.status==="new").length;$("#stat-preparing").textContent=state.orders.filter(o=>o.status==="preparing").length;$("#stat-ready").textContent=state.orders.filter(o=>o.status==="ready").length;$("#stat-today").textContent=state.orders.filter(o=>new Date(o.created_at)>=new Date(todayStart())).length}

function renderOrders(){const list=$("#orders-list"),orders=filteredOrders();if(!orders.length){list.innerHTML='<div class="empty-state">No orders in this view.</div>';return}list.innerHTML=orders.map(o=>{const t=Array.isArray(o.toppings)?o.toppings:[];return `<article class="order-card ${state.selectedId===o.id?"selected ":""}${o.status==="new"?"is-new":""}" data-id="${esc(o.id)}"><div class="order-card-top"><div><div class="order-number">${orderNumber(o.id)}</div><div class="customer">${esc(o.customer_name)}</div></div><div><span class="status-pill status-${esc(o.status)}">${statusLabel(o.status)}</span><div class="time">${formatTime(o.created_at)}</div></div></div><div class="meta"><span>${o.order_type==="delivery"?"🛵 Delivery":"🏪 Pickup"}</span><span>🍕 ${esc(o.quantity)} box${Number(o.quantity)===1?"":"es"}</span><span>${esc(o.pizza_size)}</span></div><div class="card-bottom"><div class="toppings-preview">${t.length?t.map(esc).join(", "):"No toppings"}</div><div class="total">${money(o.total)}</div></div></article>`}).join("");$$(".order-card").forEach(c=>c.addEventListener("click",()=>selectOrder(c.dataset.id)))}
function selectOrder(id){state.selectedId=id;renderOrders();renderDetails()}

function renderDetails(){const p=$("#details-panel"),o=state.orders.find(x=>x.id===state.selectedId);if(!o){p.innerHTML='<div class="details-empty"><span>🍕</span><h2>Select an order</h2><p>New orders will appear here automatically.</p></div>';return}const t=Array.isArray(o.toppings)?o.toppings:[];p.innerHTML=`<div class="details-content"><div class="details-header"><div><div class="eyebrow">ORDER ${orderNumber(o.id)}</div><h2>${esc(o.customer_name)}</h2><div class="details-sub">Received ${formatDateTime(o.created_at)}</div></div><span class="status-pill status-${esc(o.status)}">${statusLabel(o.status)}</span></div><div class="detail-section"><h3>Customer</h3><div class="detail-row"><span>Name</span><strong>${esc(o.customer_name)}</strong></div><div class="detail-row"><span>Phone</span><strong>${esc(o.customer_phone)}</strong></div><div class="detail-row"><span>Email</span><strong>${esc(o.customer_email)}</strong></div><div class="detail-links"><a href="tel:${esc(o.customer_phone)}">📞 Call</a><a href="mailto:${esc(o.customer_email)}">✉️ Email</a></div></div><div class="detail-section"><h3>Pizza</h3><div class="detail-row"><span>Size</span><strong>${esc(o.pizza_size)}</strong></div><div class="detail-row"><span>Toppings</span><strong>${esc(o.topping_count)}</strong></div><div class="detail-row"><span>Quantity</span><strong>${esc(o.quantity)}</strong></div><div class="detail-toppings">${t.length?t.map(x=>`<span class="detail-chip">${esc(x)}</span>`).join(""):'<span class="muted">None</span>'}</div></div><div class="detail-section"><h3>Order Type</h3><div class="detail-row"><span>Type</span><strong>${o.order_type==="delivery"?"Delivery":"Pickup"}</strong></div>${o.order_type==="delivery"?`<div class="instructions">${esc(o.delivery_address||"No address supplied")}</div>`:""}</div><div class="detail-section"><h3>Pricing</h3><div class="detail-row"><span>Base / unit price</span><strong>${money(o.unit_price)}</strong></div><div class="detail-row"><span>Included toppings</span><strong>${esc(o.included_toppings)}</strong></div><div class="detail-row"><span>Extra toppings</span><strong>${esc(o.extra_toppings)}</strong></div><div class="detail-row"><span>Extra topping cost</span><strong>${money(o.extra_topping_cost)}</strong></div><div class="detail-row"><span>Quantity</span><strong>${esc(o.quantity)}</strong></div><div class="detail-row"><span>Delivery fee</span><strong>${money(o.delivery_fee)}</strong></div><div class="detail-total"><span>Final total</span><strong>${money(o.total)}</strong></div></div>${o.special_instructions?`<div class="detail-section"><h3>Special Instructions</h3><div class="instructions">${esc(o.special_instructions)}</div></div>`:""}<div class="detail-section"><h3>Update Status</h3><div class="status-actions">${["new","preparing","ready","completed","cancelled"].map(s=>`<button class="status-btn ${o.status===s?"active":""}" data-status="${s}">${statusLabel(s)}</button>`).join("")}</div></div></div>`;$$(".status-btn").forEach(b=>b.addEventListener("click",()=>updateStatus(o.id,b.dataset.status)))}

async function loadOrders(){if(!supabaseClient){$("#orders-list").innerHTML='<div class="empty-state">Supabase is not configured. Add the Project URL and publishable key in dashboard.js.</div>';setConnection(false);return}const{data,error}=await supabaseClient.from("pizza_orders").select("*").order("created_at",{ascending:false});if(error){console.error(error);$("#orders-list").innerHTML='<div class="empty-state">Unable to load orders. Check Supabase configuration and staff permissions.</div>';return}state.orders=data||[];updateStats();renderOrders();renderDetails()}
function upsertOrder(o,isNew=false){const i=state.orders.findIndex(x=>x.id===o.id);if(i===-1)state.orders.push(o);else state.orders[i]=o;updateStats();if(isNew)state.selectedId=o.id;renderOrders();renderDetails();if(isNew)showNewOrderAlert(o)}

function subscribeToOrders(){if(!supabaseClient)return;if(state.channel)supabaseClient.removeChannel(state.channel);state.channel=supabaseClient.channel("pizza-orders-live").on("postgres_changes",{event:"INSERT",schema:"public",table:"pizza_orders"},p=>upsertOrder(p.new,true)).on("postgres_changes",{event:"UPDATE",schema:"public",table:"pizza_orders"},p=>upsertOrder(p.new,false)).subscribe(s=>{if(s==="SUBSCRIBED")setConnection(true);else if(["CHANNEL_ERROR","TIMED_OUT","CLOSED"].includes(s)){setConnection(false);setTimeout(()=>subscribeToOrders(),2500)}})}
async function updateStatus(id,status){if(!supabaseClient)return;const{data,error}=await supabaseClient.from("pizza_orders").update({status}).eq("id",id).select("*").single();if(error){console.error(error);showToast("Could not update the order status.");return}upsertOrder(data,false);showToast(`Order ${orderNumber(id)} marked ${statusLabel(status)}.`)}
function setupFilters(){$$("#filters .filter").forEach(b=>b.addEventListener("click",()=>{state.filter=b.dataset.filter;$$("#filters .filter").forEach(x=>x.classList.toggle("active",x===b));renderOrders()}))}
function setupSound(){$("#sound-toggle").addEventListener("click",()=>{state.soundOn=!state.soundOn;const b=$("#sound-toggle");b.textContent=state.soundOn?"🔔 Sound On":"🔕 Sound Off";b.setAttribute("aria-pressed",String(state.soundOn));if(state.soundOn)playNotification()})}
async function handleLogin(e){e.preventDefault();if(!supabaseClient){$("#login-error").textContent="Add your Supabase Project URL and publishable key to dashboard.js first.";return}const email=$("#login-email").value.trim(),password=$("#login-password").value,b=$("#login-button");b.disabled=true;b.textContent="Signing in…";$("#login-error").textContent="";const{error}=await supabaseClient.auth.signInWithPassword({email,password});if(error)$("#login-error").textContent=error.message;b.disabled=false;b.textContent="Sign In"}
async function handleLogout(){await supabaseClient?.auth.signOut()}
function showDashboard(session){$("#login-view").classList.toggle("hidden",Boolean(session));$("#dashboard-view").classList.toggle("hidden",!session);if(session){subscribeToOrders();loadOrders()}else if(state.channel&&supabaseClient){supabaseClient.removeChannel(state.channel);state.channel=null}}
async function init(){$("#login-form").addEventListener("submit",handleLogin);$("#logout-button").addEventListener("click",handleLogout);setupFilters();setupSound();if(!supabaseClient)return;const{data:{session}}=await supabaseClient.auth.getSession();showDashboard(session);supabaseClient.auth.onAuthStateChange((_e,s)=>showDashboard(s))}
init();
