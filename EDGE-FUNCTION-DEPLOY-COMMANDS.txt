PIZZA YARD — EDGE FUNCTION DEPLOYMENT (MAC)

IMPORTANT: The ZIP contains the exact folder structure required by Supabase.
You do NOT need to run supabase link again if you use the command below.

EASIEST METHOD
1. Unzip the Pizza Yard FINAL COMPLETE ZIP into Downloads.
2. Open Terminal.
3. Paste this ONE command. Replace nothing except the folder name if Finder shows a different name:

cd "/Users/dilshaunmoise/Downloads/Pizza-Yard-FINAL-COMPLETE-20260827(1)" && supabase functions deploy pizza-yard-staff-api --project-ref dsjskpqdofuhkzkylxqt --no-verify-jwt --use-api --debug

4. Press Enter once. Wait for the deployment result.

If Terminal seems to do nothing, use the Dashboard method instead.

DASHBOARD METHOD (NO TERMINAL REQUIRED)
1. Open Supabase Dashboard and select project dsjskpqdofuhkzkylxqt.
2. Open Edge Functions.
3. Open the existing pizza-yard-staff-api function.
4. Choose Edit/Deploy updates (or Deploy a new function > Via Editor if it does not exist).
5. Use the included supabase/functions/pizza-yard-staff-api/index.ts as the function source.
6. Deploy the function.

The project config intentionally uses verify_jwt = false because this function validates the staff JWT itself.
Never put a Supabase secret/service-role key into any website file.

FUNCTION URL
https://dsjskpqdofuhkzkylxqt.supabase.co/functions/v1/pizza-yard-staff-api
